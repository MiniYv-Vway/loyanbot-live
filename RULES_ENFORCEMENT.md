# 写入规则强制机制

## 机制概述
本机制通过多层防护确保所有写入操作都必须经过用户确认。

## 防护层级

### 第1层：chattr锁定（文件系统级）
- 关键文件默认使用 `chattr +i` 锁定
- 即使root也无法直接写入
- 必须由write_flow.sh解锁

### 第2层：写入流程控制
- 必须先创建写入请求
- 必须等待用户明确确认
- 必须执行写入后处理

### 第3层：违规记录与限制
- 每次违规记录到 /root/loyanbot/violation_log.json
- 违规超过3次触发写入限制
- 违规次数累积，不会重置

### 第4层：实时监控
- 文件系统监控每0.5秒检查
- 每分钟执行验证
- 自动重新锁定

## 正确流程

```
1. write_flow.sh request <文件>
   → 创建写入请求
   → 文件被锁定

2. 等待用户说"同意执行"或"确认执行"

3. write_flow.sh confirm
   → 标记请求为已确认
   → 文件解锁

4. 执行写入操作（此时文件已解锁）

5. write_flow.sh post
   → 清理状态文件
   → 文件重新锁定
```

## 查看状态

```bash
# 查看写入流程状态
write_flow.sh status

# 查看违规记录
/root/loyanbot/check_violations.sh

# 查看锁定状态
write_lock.sh status
```

## 当前状态

- 违规次数：4次
- 写入限制：已触发
- 关键文件：已锁定

## 重要警告

**任何试图绕过此机制的行为都将被记录为新的违规。**
